with open("adamek.csv", "r") as file:
    accumulator = [0, 0, 0, 0, 0]
    rr_intervals = []
    count = 0
    for elem in file:
        elem1, elem2 = elem.split()
        rr_intervals.append(float(elem1))
    
    for idx in range(rr_intervals):
        if 

        
    print(f"full number of 1s Dec: {count}")

